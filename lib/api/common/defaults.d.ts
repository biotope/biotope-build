import { Options, ParsedOptionsConfig } from './types';
export declare const defaultCliOptions: Options;
export declare const defaultConfigs: ParsedOptionsConfig;
export declare const defaultPlugins: string[];
